PORT=4000
NODE_ENV=development
MONGODB_URI=mongodb://mongodb:27017/sosabarber

JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Google OAuth (un solo cliente para todos los barberos)
# Crear en: https://console.cloud.google.com/apis/credentials → OAuth 2.0 → Aplicación web
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Callback: Google solo acepta localhost o dominio, no IP. Usar localhost (ver GOOGLE_OAUTH_SETUP.md).
GOOGLE_CALLBACK_URL=http://localhost:4000/api/auth/google/callback
# Donde redirigir al usuario tras login (tu frontend por IP o dominio)
FRONTEND_CALLBACK_URL=http://10.90.61.11
FRONTEND_URLS=http://10.90.61.11
