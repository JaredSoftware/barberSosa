# Copiar a .env y ajustar valores.
# Uso: cp .env.example .env

# MongoDB (conexión desde host para pruebas)
MONGODB_URI=mongodb://localhost:27018/sosabarber

# Backend (NestJS) - usado dentro del contenedor
PORT=4000
NODE_ENV=production

# Frontend (Nuxt) - URL del API para el navegador
NUXT_PUBLIC_API_BASE=http://localhost:4000
